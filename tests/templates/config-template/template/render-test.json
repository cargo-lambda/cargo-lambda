{
    "description": "{{ project_description }}",
    "enable_tracing": "{{ enable_tracing }}",
    "runtime": "{{ runtime }}",
    "architecture": "{{ architecture }}",
    "memory": "{{ memory }}",
    "timeout": "{{ timeout }}"
}